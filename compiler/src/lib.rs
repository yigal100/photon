pub mod ast {
    pub struct Ast {
        // AST fields
    }

    impl Ast 
    {
        pub fn new() -> Self {
            Self {
                // Initialize fields
            }
        }
        
    }

}